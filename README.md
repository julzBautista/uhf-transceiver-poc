# UHF Transceiver Proof of Concept

Custom **436 MHz UHF transceiver PCB** developed for a CubeSat communications system as part of **CalgaryToSpace**. The project focuses on RF circuit design, controlled-impedance PCB layout, power amplification, filtering, and hardware testability.

The board is built around the **Silicon Labs SI4463** transceiver and is designed to target approximately **30 dBm (1 W) RF output power** in the amateur UHF band.

## System Overview

**Transmit Path**

`SI4463 → Power Amplifier → 5th-Order Chebyshev LPF → RF Switch → Antenna`

**Receive Path**

`Antenna → RF Switch → LNA / Filtering → SI4463`

The board also integrates an **STM32F401** microcontroller for transceiver control and system interfacing.

## Design Highlights

* **4-layer RF PCB** designed in Altium Designer
* Operation around **436 MHz**
* Target transmit output power of **30 dBm**
* **SI4463** UHF transceiver
* External RF **power amplifier**
* Custom **5th-order Chebyshev low-pass filter** for PA harmonic suppression
* **50 Ω controlled-impedance coplanar waveguide (CPWG)** RF routing
* Ground stitching and via fencing around sensitive RF paths
* RF and digital sections arranged to reduce coupling and interference
* Integrated RF switching between transmit and receive paths
* Dedicated RF test points for subsystem-level characterization
* On-board power regulation for the required system rails

## PCB

The RF layout was designed with particular attention to:

* Maintaining continuous RF return paths
* Minimizing high-frequency interconnect lengths
* Controlled 50 Ω transmission lines
* Via fencing and ground stitching
* Isolation between RF, digital, and power circuitry
* Placement of matching and filtering components close to their associated RF devices

## RF Output Filtering

A custom **5th-order Chebyshev low-pass filter** was designed for the transmitter output to attenuate harmonics generated by the power amplifier while maintaining low insertion loss across the intended UHF operating band.

`PA → Chebyshev LPF → RF Switch → Antenna`

## Hardware Validation

The PCB includes accessible RF interfaces and test points to support characterization during board bring-up, including measurements of:

* Transmit output power
* Harmonic content
* Filter response
* RF signal integrity
* Power rails
* Transceiver communication and control

Testing is intended to use equipment including a **VNA, spectrum analyzer, oscilloscope, signal generator, and digital multimeter**.

## Tools

* **Altium Designer** — schematic capture and PCB layout
* **LTspice / QUCS** — circuit and RF filter simulation
* **C / Embedded Firmware** — MCU and transceiver control
* **Git** — version control

## PCB Review Notes

### Layout / Routing
- Trace clearance can be set using **net class rules** so it does not need to be adjusted manually.
- Consider removing GND copper directly underneath SMD components where appropriate. At this frequency, the effect is likely minor.
- Avoid **taps / T-junctions** in LC filters. These junctions can behave as unintended series inductance/capacitance or as band-reject stubs because the local junction impedance may differ from 50 Ω. Again, the effect is likely small at this frequency.
- For **L9 and L11**, avoid placing adjacent inductors with parallel orientations. Consider rotating one inductor by 90° to reduce magnetic cross-coupling. The impact is probably small in this application, but perpendicular placement is preferred.
- Consider merging **J1 and J3** if the current capacity of a single connector is sufficient.
- For improved **copper balance**, consider adding a copper pour on the bottom layer to reduce the risk of PCB warping during fabrication.

### Component / Schematic Changes
- **C31 and C46** may not be necessary because the corresponding filters already contain internal DC-blocking capacitors.
- **C35** may not be necessary. Lumped-element band-pass filters are typically DC-open; verify this against the specific filter datasheet.
- Consider adding additional **decoupling capacitors** close to the CTRL and supply pins of **IC1**.
- Clean up component reference designators for consistency. For example, use `Uxx` for ICs instead of mixing `Bxx`, `ICxx`, etc.

### Digital Interface
- **FIXME:** Connect the SI4463 SPI interface to the STM32's hardware SPI pins rather than using arbitrary GPIO pins.
- Consider connecting the SI4463 **Direct Mode `TX_DATA`, `RX_DATA`, and `CLK` signals** to suitable STM32 hardware peripheral pins to support continuous bit streaming.

## Project Status

**Revision 1 — Proof of Concept**

This revision is intended to validate the RF architecture, power amplifier chain, filtering, PCB layout, and overall transceiver implementation before future design iterations.

## About

This project is being developed as part of **CalgaryToSpace**, the University of Calgary's student CubeSat team. I am responsible for RF communications development and currently serve as the team's **RF Communications Technical Lead**.
